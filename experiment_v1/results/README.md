# 결과 저장 위치

`run_registry.csv`는 P0에서 초기화했다. `behavior.csv`, `semantic_metrics.csv`, `fidelity_metrics.csv`, `causal_metrics.csv`, `figures/`, `report.md`는 해당 실험이 실제로 실행된 뒤에만 생성하거나 완료 표시한다. 빈 결과 파일을 성공 증빙으로 만들지 않는다.
